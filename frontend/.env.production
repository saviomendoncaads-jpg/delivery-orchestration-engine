# Build de produção do frontend.
# VITE_BACKEND_URL vazio = o frontend usa a MESMA ORIGEM do servidor que o serve
# (o backend Express serve o build do frontend → 1 servidor, 1 URL, sem CORS).
VITE_BACKEND_URL=
